// import dateUitls from '../src/main';
// console.log(dateUitls);
// describe('date utils test', () => {
//     test('format', () => {
//         const date = new Date();
//         const dateStr1 = dateUitls.format(date);
//         const dateStr2 = date.getFullYear() + '-' + (date.getMonth() < 9 ? '0' : '') + (date.getMonth() + 1) + '-' + (date.getDate() < 10 ? '0' : '') + date.getDate();
//         expect(dateStr1).toBe(dateStr2);
//     });
  
// })

test('two plus two is four', () => {
    expect(2 + 2).toBe(4);
})